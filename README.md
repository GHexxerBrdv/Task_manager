# Usage
# run 

```bash
tik add <description> -c <category> -p <priority> --due <due_date> #to add task
tik list --c <category> #to list tasks
tik done <id> #to mark task as done
tik delete <id> #to delete task
```
